/**
 * Lógica futura para calcular o score verde de um segmento de rua.
 * Aqui depois entra a parte com Bianca .
 */

export function calculateGreenScore(segmentData: any): number {
  // Lógica mock inicial
  return Math.random() * 100; // entre 0 e 100
}
